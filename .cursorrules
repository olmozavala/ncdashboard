# Cursor Rules for ncdashboard

## Python Environment
- Always use the uv environment at `/home/olmozavala/uv/envs/eoasweb` as the default Python environment.
- When running Python code, use `uv run --python /home/olmozavala/uv/envs/eoasweb/bin/python` or activate the environment first.
- Do not create local virtual environments; use the shared uv environment instead.

## Code Style
- Always include type hints for all function arguments and return types.
- For each function, include a docstring explaining the intent, parameters, and return value.
- Follow PEP8 naming conventions.
- Do not suggest emojis for markdown files.

## Testing
- Test the code using: `uv run --python /home/olmozavala/uv/envs/eoasweb/bin/python ncdashboard_panel.py test_data/ --regex "*3z.nc"`
